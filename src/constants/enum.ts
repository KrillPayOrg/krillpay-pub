export enum ButtonT {
  default = 'default',
  submit = 'submit',
  custom = 'custom',
}

export enum FieldType {
  default = 'default',
  phone = 'phone',
  date = 'date',
  otp = 'otp',
}

export enum AccountType {
  BUSINESS = 'business',
  INDIVIDUAL = 'individual',
}

export enum ExternalTransferType {
  FUNDING = 'funding',
  INSTANT_FUNDING = 'instant_funding',
}

export enum MethodType {
  phone = 'phone',
  email = 'email',
  alternate_phone = 'alternate_phone',
}

export enum WalletType {
  USD = 'USD',
  USDC = 'USDC',
  NGN = 'NGN',
}

export enum dateType {
  start = 'start',
  end = 'end',
}

export enum KP_SY_PARAM_USER_TYPE {
  ADMIN = 'ADM',
  INDIVIDUAL = 'IND',
  BUSINESS = 'BUS',
  INDIVIDUAL_AND_BUSINESS = 'IAB',
}
